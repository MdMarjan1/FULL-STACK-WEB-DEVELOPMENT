let obj11:{name: string, role: number, present:boolean} ={
    name: 'Jekob',
    role: 566,
    present: true,
};

const {name, role} = obj11;
export{}

console.log(name,role);


let arr1:string[] = ['am', 'jam', 'ban', 'ball', 'uop']
const [frist, ,...speredOp] = arr1
console.log(frist,speredOp);

