"use strict";
let isFruit = 'Apple';
if (isFruit === 'LuisVuton') {
    const IsExpensive = true;
}
else {
    const isExpensive = false;
}
let arr1 = ['am', 'jam', 'ban', 'pop'];
for (let letter of arr1) {
    console.log(letter.toUpperCase());
}
